name: Release

on: [push]

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
          
      - name: Run release making scripts
        run: sh ./.github/workflows/sh/release.sh
